server:
  port:
    8081

spring:
  rabbitmq:
    host: localhost
    port: 5672
    username: myuser
    password: mypassword
  
  data:
    mongodb:
      host: localhost
      port: 27017
      database: sisdbmain

user:
  rabbitmq:
    queue: user
